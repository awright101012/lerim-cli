# Eval configuration — per-pipeline LLM settings and judge choice.
# Create multiple configs (eval_cheap.toml, eval_premium.toml) and compare results.

[judge]
agent = "claude"  # "claude" | "codex" | "opencode"

[extraction]
provider = "minimax"
model = "MiniMax-M2.5"

[summarization]
provider = "minimax"
model = "MiniMax-M2.5"
