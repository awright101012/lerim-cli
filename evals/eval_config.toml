# Default eval configuration.
# Copy this file to evals/configs/ and customize for model comparison.
# See docs/evals.md for full reference.

[judge]
agent = "claude"  # "claude" | "codex" | "opencode"

[extraction]
provider = "minimax"
model = "MiniMax-M2.5"
thinking = true

[summarization]
provider = "minimax"
model = "MiniMax-M2.5"
thinking = true
